#include <iostream>

int main() {

    setlocale(LC_ALL, "ru");
    int numbers, counter=0;
    std::cin >> numbers;
    while(numbers != 0)
    {
        std::cin >> numbers;
        if (numbers%5==0)
        counter++;
    }
    std::cout << counter;
    return 0;
}
