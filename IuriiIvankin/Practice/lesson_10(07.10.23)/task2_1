#include <iostream>
int F(int n)
{
    if (n == 3)
    return 1;
    else
    return F(n-1)*(n-1);
}

int main()
{
    setlocale(LC_ALL, "ru");
    int n;
    std::cin >> n;
    std::cout << "Введите значение n: ";
    std::cout << "Значение F(" << n << "): " << F(n) << std::endl;
    return 0;
}
